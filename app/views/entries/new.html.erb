<h2>New Entry</h2>

<%= simple_form_for [@organization, @logbook, @page, @entry] do |f| %>
  <div class="form-inputs">
    <%= f.input :text, :required => true, :autofocus => true %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Post entry" %>
  </div>
<% end %>
